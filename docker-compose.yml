version: '3.8'

services:
  innogw-api:
    build: .
    container_name: innogw-api
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - TZ=Asia/Bangkok
    networks:
      - innovation_net

networks:
  innovation_net:
    external: true

